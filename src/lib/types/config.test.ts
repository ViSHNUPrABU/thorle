import * as configTypes from '../types/config'

describe('types/config', () => {
  it('exports WidgetType', () => {
    expect(configTypes).toBeDefined()
  })

  it('exports VisibilityOperator', () => {
    expect(configTypes).toBeDefined()
  })

  it('exports VisibilityRule type', () => {
    expect(configTypes).toBeDefined()
  })

  it('exports ApiDataSource type', () => {
    expect(configTypes).toBeDefined()
  })

  it('exports BaseWidgetConfig type', () => {
    expect(configTypes).toBeDefined()
  })

  it('exports ChartWidgetConfig type', () => {
    expect(configTypes).toBeDefined()
  })

  it('exports TableWidgetConfig type', () => {
    expect(configTypes).toBeDefined()
  })

  it('exports DataWidgetConfig type', () => {
    expect(configTypes).toBeDefined()
  })

  it('exports WidgetConfig type', () => {
    expect(configTypes).toBeDefined()
  })

  it('exports LayoutBuilderConfig type', () => {
    expect(configTypes).toBeDefined()
  })

  it('exports DashboardLayoutConfig type', () => {
    expect(configTypes).toBeDefined()
  })

  it('exports NavItemConfig type', () => {
    expect(configTypes).toBeDefined()
  })

  it('exports AppConfig type', () => {
    expect(configTypes).toBeDefined()
  })
})
